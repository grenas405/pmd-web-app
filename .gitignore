# Runtime state: contact submissions and anything else the process writes.
var/

# Local environment overrides.
.env
.env.*

# Playwright's dependencies, fetched on demand by `deno task e2e`.
node_modules/
deno.lock.e2e

# Editor and OS noise.
.DS_Store
*.swp
.idea/
.vscode/
